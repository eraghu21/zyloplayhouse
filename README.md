# Streamlit Membership ERP

This repository contains the Streamlit Membership ERP application with OTP login, PDF certificate generation, QR check-in, and SMTP email features.

## Run locally
1. Create a virtualenv (recommended)
2. pip install -r requirements.txt
3. streamlit run app.py

## Deployment
See Dockerfile / Procfile for Docker / Heroku deployment.

